---
phase: 03-relat-rio-pdf-e-pipeline-de-ia
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - worker/index.ts
  - worker/processors/ai-describe-item.ts
  - src/app/api/vistorias/[id]/itens/[itemId]/midia/route.ts
  - src/app/api/vistorias/[id]/route.ts
  - src/lib/db/idb.ts
  - src/app/field/vistorias/[id]/resumo/page.tsx
  - env.example
autonomous: true
requirements:
  - SC-01
  - PIPE-01
must_haves:
  truths:
    - "Uploading photo+audio for an item enqueues an ai-describe-item BullMQ job outside the HTTP hot path completion critical section (fire-and-forget after DB write)"
    - "Worker transcribes audio, generates description, persists AnaliseIa and updates Item.descricao/descricaoFinal/estadoConservacao/status=ANALISADO"
    - "Worker reads AIRouter results as { data, meta } and maps meta to AnaliseIa fields"
    - "Worker rejects jobs when item.ambiente.vistoriaId !== job.vistoriaId (UnrecoverableError)"
    - "Media POST verifies item belongs to path vistoriaId before create"
    - "Field finalize uses dedicated UPDATE_VISTORIA_STATUS (or FINALIZAR_CAMPO) that only sets Vistoria.status → EM_REVISAO — never status-only via UPDATE_CHECKLIST"
    - "Failed AI jobs leave item recoverable (retry via BullMQ) without crashing the worker process"
    - "Item status transitions CAPTURADO → EM_ANALISE → ANALISADO on success"
  artifacts:
    - path: "worker/index.ts"
      provides: "BullMQ workers entrypoint for npm run worker"
      min_lines: 20
    - path: "worker/processors/ai-describe-item.ts"
      provides: "Two-stage STT + multimodal pipeline processor"
    - path: "src/app/api/vistorias/[id]/itens/[itemId]/midia/route.ts"
      provides: "Media upload that marks CAPTURADO and enqueues AI when ready"
    - path: "src/app/api/vistorias/[id]/route.ts"
      provides: "UPDATE_VISTORIA_STATUS / FINALIZAR_CAMPO dedicated status action"
    - path: "src/lib/db/idb.ts"
      provides: "Mutation action + LocalVistoria.status include EM_REVISAO / UPDATE_VISTORIA_STATUS"
  key_links:
    - from: "src/app/api/vistorias/[id]/itens/[itemId]/midia/route.ts"
      to: "enqueueAiDescribeItem"
      via: "after prisma.midia.create when item has FOTO+AUDIO"
      pattern: "enqueueAiDescribeItem"
    - from: "worker/processors/ai-describe-item.ts"
      to: "AIRouter"
      via: "transcribeAudio then generateDescription → .data / .meta"
      pattern: "transcribeAudio|generateDescription|\\.data|\\.meta"
    - from: "worker/processors/ai-describe-item.ts"
      to: "prisma.analiseIa.create"
      via: "persist provider output from meta"
      pattern: "analiseIa\\.create"
    - from: "src/app/field/vistorias/[id]/resumo/page.tsx"
      to: "UPDATE_VISTORIA_STATUS"
      via: "mutation_queue on finalize"
      pattern: "UPDATE_VISTORIA_STATUS|FINALIZAR_CAMPO"
---

<objective>
Wire the async AI pipeline: on media sync, enqueue BullMQ jobs; worker runs STT → multimodal description → Prisma persistence with status transitions. Fix field finalize so status-only updates never go through UPDATE_CHECKLIST.

Purpose: Deliver SC-01 (audio transcribed + editable technical description from photo+speech) without blocking API requests; keep checklist data safe on field finish.
Output: `worker/*` processors, media route enqueue, dedicated vistoria status action, item status updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-relat-rio-pdf-e-pipeline-de-ia/03-AI-SPEC.md
@.planning/phases/03-relat-rio-pdf-e-pipeline-de-ia/03-01-PLAN.md
@.planning/phases/03-relat-rio-pdf-e-pipeline-de-ia/03-02-PLAN.md
@src/app/api/vistorias/[id]/itens/[itemId]/midia/route.ts
@src/app/api/vistorias/[id]/route.ts
@src/lib/db/idb.ts
@src/app/field/vistorias/[id]/resumo/page.tsx
@src/hooks/useSyncEngine.ts
@package.json
</context>

<interfaces>
From Plan 01:
- enqueueAiDescribeItem({ itemId, vistoriaId })
- QUEUE_AI_DESCRIBE = "ai-describe-item"
- resolveLocalUploadPath(midiaUrl)

From Plan 02 (LOCKED — no alternatives):
```ts
type AiRunMeta = {
  provider: "GEMINI" | "OPENAI" | "CLAUDE";
  usedFallback: boolean;
  latencyMs: number;
  guardrailHit: boolean;
};
type AiResult<T> = { data: T; meta: AiRunMeta };

// BOTH methods return AiResult — never bare values, never lastRunMeta
router.transcribeAudio(buffer, mimeType) → Promise<AiResult<string>>
router.generateDescription(photoBuffer, mimeType, transcript) → Promise<AiResult<DescriptionOutput>>
mapItemStateToPrisma(state)
// generateDescription already downscales photos to max 1024x1024 internally
```

From package.json:
- "worker": "tsx worker/index.ts"

Current midia route writes to public/uploads/{vistoriaId}/{filename} and prisma.midia.create with syncStatus SYNCED — WITHOUT verifying item ownership today (must fix).

Current resumo handleFinalize incorrectly queues:
  action: "UPDATE_CHECKLIST", payload: { status: "CONCLUIDA" }
which would wipe ChecklistChegada fields to undefined on upsert — FORBIDDEN. Replace with dedicated status action.

Current UPDATE_CHECKLIST in route.ts only upserts checklist boolean fields — do NOT overload it for vistoria status.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement AI describe item processor</name>
  <files>worker/processors/ai-describe-item.ts</files>
  <read_first>
    - .planning/phases/03-relat-rio-pdf-e-pipeline-de-ia/03-AI-SPEC.md
    - src/lib/ai/router.ts
    - src/lib/ai/schemas.ts
    - src/lib/queue/jobs.ts
    - src/lib/storage/index.ts
    - prisma/schema.prisma
    - src/lib/db/index.ts
  </read_first>
  <action>
    Create worker/processors/ai-describe-item.ts exporting async function processAiDescribeItem(job: Job&lt;AiDescribeItemJob&gt;): Promise&lt;void&gt;.

    Algorithm (must be linear two-stage, no agent framework):
    1. Read job.data.itemId and vistoriaId. Load Item via prisma.item.findUnique with include: { midias: true, ambiente: { select: { vistoriaId: true } } }. If item missing, throw UnrecoverableError (from bullmq) so job is not retried forever.
    2. Tenant bind (T-03-13): if !item.ambiente || item.ambiente.vistoriaId !== job.data.vistoriaId, throw UnrecoverableError("Item does not belong to vistoria") — do not process cross-tenant jobs.
    3. Set item.status = EM_ANALISE.
    4. Select latest AUDIO midia and latest FOTO midia. If either missing, set status back to CAPTURADO (or leave CAPTURADO) and return early without throwing (not ready).
    5. Load file bytes:
       - Prefer reading local file via resolveLocalUploadPath(midia.url) if file exists under public/.
       - Else if storage key present, use getStorageProvider to fetch (getSignedUrl + fetch, or GetObject if implemented). If GetObject not on interface, add a download(key): Promise&lt;Buffer&gt; method to StorageProvider in this task (allowed small extension to Plan 01 types) OR fetch signed URL.
    6. Stage 1 STT: const stt = await AIRouter.transcribeAudio(audioBuffer, mime); use stt.data as transcript string; stt.meta for logging. Save transcript on audio Midia.transcricao via prisma.midia.update.
    7. Stage 2: const desc = await AIRouter.generateDescription(photoBuffer, imageMime, stt.data). Router already downscales to 1024x1024 — do not skip. Use desc.data.itemState / desc.data.technicalDescription and desc.meta for AnaliseIa.
    8. Persist AnaliseIa: provedor mapped from desc.meta.provider (GEMINI|OPENAI|CLAUDE), input: { transcript: stt.data, photoKey, promptsVersion: "phase3-v1" }, output: desc.data JSON, analise: technicalDescription, custo/tokens if available, latencyMs: desc.meta.latencyMs, usedFallback: desc.meta.usedFallback, guardrailHit: desc.meta.guardrailHit, stage: "full".
    9. Update Item: descricao = technicalDescription, descricaoFinal = technicalDescription (editable baseline), estadoConservacao = mapItemStateToPrisma(itemState), status = ANALISADO, analisadoEm = now(), descricaoEditada = false.
    10. On provider failure: rethrow to let BullMQ retry; do not mark ANALISADO. After final failure BullMQ will exhaust attempts — optionally set status CAPTURADO with console.error (document choice in SUMMARY).
    11. Never write the words laudo/perícia into prompts beyond the ban instruction; never store un-sanitized regulated terms (router already enforces).

    Do not generate PDF here.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | Select-Object -First 40</automated>
  </verify>
  <acceptance_criteria>
    - worker/processors/ai-describe-item.ts exists and exports processAiDescribeItem
    - Source calls transcribeAudio and generateDescription in that order
    - Source reads .data and .meta from both AIRouter results (not bare string / lastRunMeta)
    - Source includes ambiente in findUnique and compares ambiente.vistoriaId to job vistoriaId
    - Source throws UnrecoverableError (or equivalent non-retry poison) on tenant mismatch
    - Source writes Midia.transcricao (prisma.midia.update with transcricao)
    - Source creates analiseIa record (prisma.analiseIa.create) using meta.latencyMs / usedFallback / guardrailHit
    - Source sets status to EM_ANALISE before AI and ANALISADO after success
    - No imports of langchain/langgraph/crewai
  </acceptance_criteria>
  <done>Processor implements STT → description → Prisma persistence with status machine, tenant bind, and { data, meta } contract.</done>
</task>

<task type="auto">
  <name>Task 2: Create worker entrypoint (npm run worker)</name>
  <files>worker/index.ts, env.example</files>
  <read_first>
    - worker/processors/ai-describe-item.ts
    - src/lib/queue/connection.ts
    - src/lib/queue/jobs.ts
    - package.json
    - docker-compose.yml
  </read_first>
  <action>
    Create worker/index.ts as the process started by `npm run worker` / docker-compose worker service.

    Requirements:
    1. Import 'Worker' from bullmq, getRedisConnection, QUEUE_AI_DESCRIBE (and register QUEUE_GENERATE_PDF with a stub processor that logs "PDF processor not registered yet" OR skip PDF worker until Plan 05 — prefer registering only AI queue now and leave a TODO comment for PDF).
    2. Instantiate Worker(QUEUE_AI_DESCRIBE, processAiDescribeItem, { connection, concurrency: 2 }).
    3. Log worker ready on start; log job completed/failed with job.id and itemId (no base64).
    4. Handle SIGINT/SIGTERM: await worker.close() then process.exit(0).
    5. Ensure tsx can resolve @/* imports: if path aliases fail under tsx, use relative imports from worker to src/lib OR add tsconfig-paths/register. Prefer configuring worker imports that tsx resolves — check if project already supports paths; if not, use relative imports like `../src/lib/ai` from worker files to avoid new deps.
    6. env.example: document `npm run worker` requires REDIS_URL + AI keys (GEMINI_API_KEY at minimum).

    Do not start Next.js server from worker.
  </action>
  <verify>
    <automated>node -e "require('fs').accessSync('worker/index.ts'); console.log('ok')" && npx tsc --noEmit 2>&1 | Select-Object -First 40</automated>
  </verify>
  <acceptance_criteria>
    - worker/index.ts exists
    - worker/index.ts constructs a bullmq Worker for queue name "ai-describe-item"
    - package.json script "worker" remains "tsx worker/index.ts"
    - Graceful shutdown handlers for SIGINT or SIGTERM present
    - File does not import next/server
  </acceptance_criteria>
  <done>Standalone worker process can consume AI jobs from Redis.</done>
</task>

<task type="auto">
  <name>Task 3: Media enqueue, ownership checks, and dedicated field-finalize status action</name>
  <files>src/app/api/vistorias/[id]/itens/[itemId]/midia/route.ts, src/app/api/vistorias/[id]/route.ts, src/lib/db/idb.ts, src/app/field/vistorias/[id]/resumo/page.tsx</files>
  <read_first>
    - src/app/api/vistorias/[id]/itens/[itemId]/midia/route.ts
    - src/app/api/vistorias/[id]/route.ts
    - src/lib/db/idb.ts
    - src/app/field/vistorias/[id]/resumo/page.tsx
    - src/lib/queue/queues.ts
    - src/hooks/useSyncEngine.ts
    - src/lib/auth.ts
    - prisma/schema.prisma
  </read_first>
  <action>
    ### A) Media upload POST (ownership + enqueue)
    Update src/app/api/vistorias/[id]/itens/[itemId]/midia/route.ts:
    1. Keep auth check via getServerSession(authOptions); 401 if missing.
    2. BEFORE prisma.midia.create: load item with ambiente (and vistoria/empresa if needed). Verify item exists and item.ambiente.vistoriaId === vistoriaId from the URL path. If not, return 404 (or 403). Optionally also verify session.user.empresaId matches vistoria.empresaId when session exposes empresaId.
    3. After successful prisma.midia.create, update Item.status to CAPTURADO if it was PENDENTE or still CAPTURADO.
    4. Query item midias; if at least one FOTO and one AUDIO exist for itemId, call enqueueAiDescribeItem({ itemId, vistoriaId }). Prefer: await enqueue; on enqueue failure still return 201 with warning field `aiEnqueue: false` so media is not lost (media save is source of truth). Document in SUMMARY.
    5. Do not run AIRouter inside the route.

    ### B) Dedicated vistoria status action (CRITICAL — checklist safety)
    Current bug: resumo queues UPDATE_CHECKLIST with payload { status: "CONCLUIDA" }. UPDATE_CHECKLIST upserts ChecklistChegada with only the payload fields, which would null/wipe checklist booleans. FORBIDDEN.

    In src/app/api/vistorias/[id]/route.ts:
    1. Add action branch `UPDATE_VISTORIA_STATUS` (alias also accept `FINALIZAR_CAMPO` as the same handler if simpler for field code).
    2. Payload: { status: string }. Allowed mapping:
       - "EM_REVISAO" → StatusVistoria.EM_REVISAO
       - "CONCLUIDA" (legacy field value) → StatusVistoria.EM_REVISAO (field finished capture, awaiting AI/review)
       - Reject or ignore attempts to set FINALIZADA via this field path (FINALIZADA is Plan 05 post-review only). Return 400 if payload.status is FINALIZADA.
    3. Handler MUST only call prisma.vistoria.update({ where: { id: vistoriaId }, data: { status: mapped } }) — do NOT touch ChecklistChegada, items, or midias.
    4. Keep existing UPDATE_CHECKLIST behavior for real checklist boolean payloads only. Do NOT add status handling inside UPDATE_CHECKLIST.
    5. When action UPDATE_ITEM_STATUS: ensure item belongs to vistoriaId in the URL (load via ambiente.vistoriaId) before update; 404/403 otherwise.

    ### C) IndexedDB types
    In src/lib/db/idb.ts:
    1. Extend MutationQueueItem.action union with "UPDATE_VISTORIA_STATUS" (and "FINALIZAR_CAMPO" only if used — prefer single name UPDATE_VISTORIA_STATUS).
    2. Extend LocalVistoria.status union: add "EM_REVISAO"; keep "CONCLUIDA" only as transitional local value if needed, but prefer switching local writes to "EM_REVISAO" so field UI matches server. Map any remaining "CONCLUIDA" displays as finished-in-field if UI reads status chips.

    ### D) Field resumo finalize
    In src/app/field/vistorias/[id]/resumo/page.tsx handleFinalize:
    1. Set local vistoria.status = "EM_REVISAO" (not "CONCLUIDA").
    2. Queue mutation: action "UPDATE_VISTORIA_STATUS", payload: { status: "EM_REVISAO" } — NEVER UPDATE_CHECKLIST with status-only payload.
    3. Keep navigate to sucesso.

    If useSyncEngine switch/cases list allowed actions, add UPDATE_VISTORIA_STATUS passthrough to PUT /api/vistorias/[id] (read useSyncEngine and update if it filters actions).

    Security: only session users; never process other empresas' items.
  </action>
  <verify>
    <automated>rg -n "enqueueAiDescribeItem" "src/app/api/vistorias" && rg -n "UPDATE_VISTORIA_STATUS|FINALIZAR_CAMPO|EM_REVISAO" "src/app/api/vistorias" "src/lib/db/idb.ts" "src/app/field/vistorias" && rg -n "ambiente\.vistoriaId|vistoriaId" "src/app/api/vistorias/[id]/itens/[itemId]/midia"</automated>
  </verify>
  <acceptance_criteria>
    - midia/route.ts imports and calls enqueueAiDescribeItem
    - midia/route.ts verifies item.ambiente.vistoriaId === path vistoriaId before midia.create
    - midia/route.ts updates item status to CAPTURADO after upload
    - Enqueue is gated on presence of both FOTO and AUDIO for the item
    - No direct GoogleGenerativeAI or OpenAI calls inside midia/route.ts
    - route.ts has UPDATE_VISTORIA_STATUS (or FINALIZAR_CAMPO) branch that only updates Vistoria.status
    - UPDATE_CHECKLIST branch does not read or write payload.status for vistoria completion
    - idb MutationQueueItem.action includes UPDATE_VISTORIA_STATUS
    - resumo handleFinalize does not use action UPDATE_CHECKLIST for finalize
    - resumo does not write local status "CONCLUIDA" (uses EM_REVISAO)
    - Field completion path does not set StatusVistoria to FINALIZADA
  </acceptance_criteria>
  <done>Media sync triggers async AI with ownership checks; field finish uses a dedicated status-only action to EM_REVISAO without wiping checklist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP upload → disk/DB → queue | Authenticated user media becomes job input |
| Worker → AI providers | Media bytes sent to third parties |
| Worker → PostgreSQL | Generated text written as item descriptions |
| Field mutation queue → API | Offline mutations must not corrupt checklist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Spoofing | Media upload API | mitigate | Require NextAuth session; bind itemId to vistoriaId path param before create |
| T-03-10 | Tampering | Job payload itemId | mitigate | Worker reloads Item/Midia from Prisma; ignores client-supplied file paths in job |
| T-03-11 | Information Disclosure | Worker logs | mitigate | Log ids/status only; never log full transcripts in production info level if they contain PII addresses — allow transcript in AnaliseIa.input for audit |
| T-03-12 | Denial of Service | AI fan-out | mitigate | BullMQ concurrency 2; 3 attempts with exponential backoff |
| T-03-13 | Elevation of Privilege | Cross-tenant item | mitigate | Validate item.ambiente.vistoriaId === job.vistoriaId before processing; UnrecoverableError on mismatch |
| T-03-18 | Tampering | Field finalize mutation | mitigate | Dedicated UPDATE_VISTORIA_STATUS only updates Vistoria.status; never reuse UPDATE_CHECKLIST for status-only payloads |
| T-03-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
- Worker entry compiles and registers ai-describe-item
- Media route enqueues jobs when foto+audio present and verifies ownership
- Processor persists AnaliseIa + Item ANALISADO fields from { data, meta }
- Field finalize uses UPDATE_VISTORIA_STATUS → EM_REVISAO
</verification>

<success_criteria>
End-to-end async path exists: sync media → queue → STT+description → editable Item fields in DB; field finish safely moves vistoria to EM_REVISAO without checklist wipe.
</success_criteria>

<output>
Create `.planning/phases/03-relat-rio-pdf-e-pipeline-de-ia/03-03-SUMMARY.md` when done
</output>
