---
phase: 03-relat-rio-pdf-e-pipeline-de-ia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/storage/types.ts
  - src/lib/storage/s3.ts
  - src/lib/storage/index.ts
  - src/lib/queue/connection.ts
  - src/lib/queue/queues.ts
  - src/lib/queue/jobs.ts
  - env.example
autonomous: true
requirements:
  - SC-01
  - SC-03
  - PIPE-01
must_haves:
  truths:
    - "Schema supports item conservation state, audio transcription storage, and EM_REVISAO vistoria status"
    - "StorageProvider can upload/getSignedUrl/delete against MinIO or R2 based on STORAGE_PROVIDER"
    - "BullMQ queues ai-describe-item and generate-pdf are defined with typed job payloads"
    - "Prisma client is regenerated and schema is pushed to the database"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Item.estadoConservacao, Midia.transcricao, StatusVistoria.EM_REVISAO"
      contains: "estadoConservacao"
    - path: "src/lib/storage/index.ts"
      provides: "getStorageProvider factory"
      exports: ["getStorageProvider"]
    - path: "src/lib/queue/queues.ts"
      provides: "AI and PDF queue instances"
      exports: ["aiDescribeQueue", "generatePdfQueue"]
  key_links:
    - from: "src/lib/storage/index.ts"
      to: "process.env.STORAGE_PROVIDER"
      via: "factory switch r2|minio"
      pattern: "STORAGE_PROVIDER"
    - from: "src/lib/queue/connection.ts"
      to: "process.env.REDIS_URL"
      via: "ioredis connection"
      pattern: "REDIS_URL"
---

<objective>
Extend the Prisma schema for AI pipeline + review states, implement the S3-compatible storage abstraction, and define BullMQ queue contracts used by the worker and finalize flow.

Purpose: Foundation required before AI router, worker, review UI, and PDF generation can run against real types and infrastructure.
Output: Updated schema (pushed), `src/lib/storage/*`, `src/lib/queue/*`, env.example notes for Redis/storage/AI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/STACK.md
@.planning/INTEGRATIONS.md
@.planning/phases/03-relat-rio-pdf-e-pipeline-de-ia/03-AI-SPEC.md
@SYSTEM_DESIGN.md
@prisma/schema.prisma
@env.example
@package.json
</context>

<interfaces>
<!-- Existing contracts the executor must respect -->

From prisma/schema.prisma (current):
- model Item: descricao, descricaoFinal, descricaoEditada, status StatusItem, analisadoEm, midias, analisesIa
- model Midia: tipo TipoMidia (FOTO|AUDIO), url, key, itemId, syncStatus
- model AnaliseIa: provedor ProvedorIa, input Json, output Json, analise String, confianca, custo, tokens
- model Relatorio: pdfStorageKey, urlPublica, status StatusRelatorio, geradoEm, ...
- model Vistoria: tokenPublico, expiracaoToken, relatorio Relatorio?
- enum StatusItem: PENDENTE | CAPTURADO | EM_ANALISE | ANALISADO | REVISADO | FINALIZADO
- enum StatusVistoria: AGENDADA | EM_ANDAMENTO | FINALIZADA | CANCELADA
- enum ProvedorIa: GEMINI | OPENAI | CLAUDE
- enum StatusRelatorio: GERADO | ENVIADO | VISUALIZADO | CONFIRMADO

From SYSTEM_DESIGN.md §5:
```ts
interface StorageProvider {
  upload(key: string, file: Buffer, contentType: string): Promise<string>;
  getSignedUrl(key: string, expiresInSec?: number): Promise<string>;
  delete(key: string): Promise<void>;
}
```

From package.json scripts:
- "worker": "tsx worker/index.ts"
- deps present: bullmq, ioredis, @aws-sdk/client-s3, @aws-sdk/s3-request-presigner
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend Prisma schema for AI pipeline and review</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma
    - .planning/phases/03-relat-rio-pdf-e-pipeline-de-ia/03-AI-SPEC.md
    - SYSTEM_DESIGN.md
  </read_first>
  <action>
    Modify prisma/schema.prisma only (no other files). Apply these concrete schema changes:

    1. Add enum EstadoConservacao with values: NOVO, BOM, REGULAR, RUIM (maps AI-SPEC DescriptionOutput.itemState Novo/Bom/Regular/Ruim).

    2. On model Item add:
       - estadoConservacao EstadoConservacao? @map("estado_conservacao")
       Keep existing descricao, descricaoFinal, descricaoEditada, status, analisadoEm unchanged.

    3. On model Midia add:
       - transcricao String? @db.Text
       (stores STT text for AUDIO midias; null for FOTO)

    4. On enum StatusVistoria add EM_REVISAO between EM_ANDAMENTO and FINALIZADA so order is: AGENDADA, EM_ANDAMENTO, EM_REVISAO, FINALIZADA, CANCELADA.

    5. On model AnaliseIa add optional observability fields used by AI-SPEC §7 without a separate AIPipelineLog model:
       - latencyMs Int? @map("latency_ms")
       - usedFallback Boolean @default(false) @map("used_fallback")
       - guardrailHit Boolean @default(false) @map("guardrail_hit")
       - stage String? (e.g. "stt" | "describe" | "full")

    6. Do NOT rename Relatorio to anything containing "laudo". Do NOT add WhatsApp or Contestacao fields (Phase 4). Do NOT remove existing models.

    7. Run `npx prisma validate` after edits (validation only — push is Task 3).
  </action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains `enum EstadoConservacao` with NOVO, BOM, REGULAR, RUIM
    - model Item contains field `estadoConservacao`
    - model Midia contains field `transcricao`
    - enum StatusVistoria contains `EM_REVISAO`
    - model AnaliseIa contains fields `latencyMs`, `usedFallback`, `guardrailHit`, `stage`
    - `npx prisma validate` exits 0
    - No identifier in schema uses the substrings laudo or pericia (case-insensitive) except possibly comments that ban them — prefer zero occurrences of those words as model/field names
  </acceptance_criteria>
  <done>Schema extended for AI output state, transcription, review status, and pipeline observability.</done>
</task>

<task type="auto">
  <name>Task 2: Implement StorageProvider and BullMQ queue contracts</name>
  <files>src/lib/storage/types.ts, src/lib/storage/s3.ts, src/lib/storage/index.ts, src/lib/queue/connection.ts, src/lib/queue/queues.ts, src/lib/queue/jobs.ts, env.example</files>
  <read_first>
    - SYSTEM_DESIGN.md
    - .planning/INTEGRATIONS.md
    - env.example
    - package.json
    - src/lib/db/index.ts
  </read_first>
  <action>
    Create storage and queue modules under src/lib (path alias `@/*` → `./src/*`). Ignore empty root `lib/ai` and `lib/storage` folders; do not put production code there.

    Storage:
    1. Create src/lib/storage/types.ts exporting interface StorageProvider with methods: upload(key, file: Buffer, contentType): Promise&lt;string&gt; (returns public or app-relative URL string), getSignedUrl(key, expiresInSec?: number): Promise&lt;string&gt;, delete(key): Promise&lt;void&gt;.
    2. Create src/lib/storage/s3.ts implementing StorageProvider via @aws-sdk/client-s3 S3Client + PutObjectCommand, DeleteObjectCommand, and @aws-sdk/s3-request-presigner getSignedUrl.
       - When process.env.STORAGE_PROVIDER === "minio": endpoint = process.env.MINIO_ENDPOINT, credentials MINIO_ACCESS_KEY / MINIO_SECRET_KEY, forcePathStyle true, bucket = process.env.STORAGE_BUCKET || "vistorias".
       - When STORAGE_PROVIDER === "r2" (default): endpoint = process.env.R2_ENDPOINT, credentials R2_ACCESS_KEY_ID / R2_SECRET_ACCESS_KEY, bucket = process.env.STORAGE_BUCKET || "app-facilita".
    3. Create src/lib/storage/index.ts exporting getStorageProvider(): StorageProvider singleton.
    4. Also export a helper resolveLocalUploadPath(midiaUrl: string): string that maps app-relative urls starting with /uploads/ to path.join(process.cwd(), "public", midiaUrl) so the worker can read files written by the current midia route until full S3 migration is used.

    Queue:
    1. Create src/lib/queue/connection.ts exporting a function getRedisConnection() returning ioredis options or connection derived from process.env.REDIS_URL (default redis://127.0.0.1:6379 for local host dev).
    2. Create src/lib/queue/jobs.ts exporting typed payloads:
       - AiDescribeItemJob = { itemId: string; vistoriaId: string }
       - GeneratePdfJob = { vistoriaId: string; requestedByUserId?: string }
       - Queue name constants: QUEUE_AI_DESCRIBE = "ai-describe-item", QUEUE_GENERATE_PDF = "generate-pdf"
    3. Create src/lib/queue/queues.ts exporting aiDescribeQueue and generatePdfQueue as BullMQ Queue instances (lazy or module-level) using getRedisConnection(), with default job options: attempts 3, backoff exponential delay 5000, removeOnComplete 100, removeOnFail 200.
    4. Export async function enqueueAiDescribeItem(data: AiDescribeItemJob) and enqueueGeneratePdf(data: GeneratePdfJob) that add jobs with jobId based on itemId or vistoriaId to avoid duplicates (use jobId `ai-${itemId}` and `pdf-${vistoriaId}`).

    env.example:
    - Ensure REDIS_URL, STORAGE_PROVIDER, STORAGE_BUCKET, MinIO and R2 vars remain documented (already present).
    - Add a short comment block above AI vars noting worker must run via `npm run worker` and depends on Redis.
    - Do not put real secrets.

    Do not implement the worker processor yet (Plan 03). Do not implement AIRouter yet (Plan 02).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | Select-String -Pattern "src/lib/storage|src/lib/queue" -SimpleMatch; if (-not $?) { npx tsc --noEmit --pretty false 2>&1 | Select-Object -First 40 }</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: src/lib/storage/types.ts, src/lib/storage/s3.ts, src/lib/storage/index.ts, src/lib/queue/connection.ts, src/lib/queue/queues.ts, src/lib/queue/jobs.ts
    - src/lib/storage/types.ts exports name StorageProvider
    - src/lib/storage/index.ts exports getStorageProvider
    - src/lib/queue/jobs.ts exports QUEUE_AI_DESCRIBE string equal to "ai-describe-item" and QUEUE_GENERATE_PDF equal to "generate-pdf"
    - src/lib/queue/queues.ts exports enqueueAiDescribeItem and enqueueGeneratePdf
    - No import of LangChain, LangGraph, CrewAI, or Vercel AI SDK in these files
    - `rg "laudo|per[ií]cia" src/lib/storage src/lib/queue` returns no matches (or only comments banning the terms)
  </acceptance_criteria>
  <done>Storage and queue contracts exist and compile; ready for AI router and worker consumers.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Prisma generate and db push</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma
    - package.json
    - env.example
  </read_first>
  <action>
    [BLOCKING] After Task 1 schema modifications are complete, apply schema to the database and regenerate the client BEFORE any later plan verification depends on new fields.

    1. Run `npx prisma generate`.
    2. Run `npx prisma db push` against the configured DATABASE_URL.
       - Prefer non-interactive `npx prisma db push` (no data-loss flag) because new fields are additive/nullable.
       - Only if Prisma requires confirmation for an unavoidable destructive change, stop and set autonomous false with a clear message — do not silently pass `--accept-data-loss`.
    3. If PostgreSQL is not running, start it with `docker compose up -d postgres` (and redis if convenient) then retry push.
    4. Confirm generate output mentions client generated and push reports the database is in sync (or lists the additive changes applied).

    Do not create a new migration folder unless the project is already on migrate-only workflow for this env; package.json has both migrate and the orchestrator explicitly allows `db push` for this phase.
  </action>
  <verify>
    <automated>npx prisma generate && npx prisma db push</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma generate` exits 0
    - `npx prisma db push` exits 0
    - Generated client types include EstadoConservacao and Item.estadoConservacao (verifiable by `rg "estadoConservacao" node_modules/.prisma/client/index.d.ts` or equivalent)
    - No destructive `--accept-data-loss` used unless explicitly documented in the plan SUMMARY with justification
  </acceptance_criteria>
  <done>Database and Prisma Client match the Phase 3 schema extensions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Redis | Job payloads cross into shared infrastructure |
| App → Object storage (MinIO/R2) | Media bytes and keys leave the app process |
| Env secrets → runtime | STORAGE and REDIS credentials configure clients |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Storage signed URLs | mitigate | Default short TTL on getSignedUrl (e.g. 3600s); never log secret keys |
| T-03-02 | Tampering | BullMQ job payloads | mitigate | Typed payloads only; job processors (later plans) must re-load Item/Midia from Prisma by id, never trust free-form paths from the job |
| T-03-03 | Elevation of Privilege | Storage credentials | mitigate | Read credentials only from env; no hard-coded keys in source |
| T-03-SC | Tampering | npm installs | accept | No new packages in this plan — uses already-installed @aws-sdk/*, bullmq, ioredis |
</threat_model>

<verification>
- prisma validate + generate + db push succeed
- storage and queue modules exist with required exports
- typecheck does not fail on new modules
</verification>

<success_criteria>
Schema supports AI pipeline fields; storage and queue contracts are importable by subsequent plans; DB is pushed.
</success_criteria>

<output>
Create `.planning/phases/03-relat-rio-pdf-e-pipeline-de-ia/03-01-SUMMARY.md` when done
</output>
