# Phase 3.1 — Produtividade em Campo — Summary

**Completed:** 2026-07-17  
**Status:** Implemented (human UAT recommended on device)

## Delivered

### Plan 01 — Templates
- `src/lib/field/templates.ts` — Studio, Apto 1/2/3, Casa + `defaultItemsForRoomName`
- `src/lib/field/applyTemplate.ts` — apply property template / create room with default items offline
- UI picker on `ambientes/page.tsx` when 0 rooms
- “+ Cômodo” creates default items for room type
- API PUT: `CREATE_AMBIENTE_LOCAL`, `CREATE_ITEM_LOCAL` (+ legacy aliases)

### Plan 02 — Navegação + foto obrigatória (D-05)
- `src/lib/field/itemNavigation.ts`
- Capture: ≥1 foto required to save; “Salvar e próximo” / “Salvar e voltar à lista”
- Ambiente list: “Continuar vistoria” CTA (already present, label updated)

### Plan 03 — Score acionável
- `src/lib/field/completionScore.ts` — % + blockers (incl. missing photo) + `canFinalize`
- Resumo: issues list with deep links; finalize gated on photo + status
- Ambientes progress card reuses completion weights (protocolo / itens / fotos)

## Tests
- `tests/field/completionScore.test.ts` — 6 passing

## Deferred
- Medidores, finalize PDF no field, Phase 4 WhatsApp/contestação
- Template re-apply / bulk delete rooms
- CREATE_VISTORIA_LOCAL full server upsert (local-only inspections may still 404 on sync until vistoria exists server-side)
